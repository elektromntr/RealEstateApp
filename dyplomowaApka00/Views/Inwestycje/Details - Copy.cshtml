@model dyplomowaApka00.Models.Inwestycja

@{
    ViewBag.Title = "Details";
}

<h2>@Html.DisplayFor(model => model.Nazwa)</h2>

<div>
    <h4>Inwestycja</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nazwa)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nazwa)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.InwestycjaId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
<div>
    @{ Html.RenderAction("MieszkaniaZInwestycji"@*metoda*@, "Mieszkania"@*kontroler*@, new { id = Model.InwestycjaId }); }
</div>
<div>
    @if (Model.Domy != null && Model.Domy.Any())
    {
        { Html.RenderAction("DomyZInwestycji", "Domy", new { id = Model.InwestycjaId }); }
    }
</div>